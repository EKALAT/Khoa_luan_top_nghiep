Future<void> downloadFile({
  required List<int> bytes,
  required String filename,
  String mimeType = 'application/octet-stream',
}) async {
  throw UnsupportedError(
    'Tai file truc tiep hien chi duoc bat cho Flutter Web.',
  );
}
